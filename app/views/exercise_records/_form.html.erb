<%= form_with(model: exercise_record, local: true) do |form| %>
  <% if exercise_record.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(exercise_record.errors.count, "error") %> prohibited this exercise_record from being saved:</h2>

      <ul>
      <% exercise_record.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.number_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :exercise_id %>
    <%= form.number_field :exercise_id %>
  </div>

  <div class="field">
    <%= form.label :num_reps %>
    <%= form.number_field :num_reps %>
  </div>

  <div class="field">
    <%= form.label :weight %>
    <%= form.number_field :weight %>
  </div>

  <div class="field">
    <%= form.label :num_sets %>
    <%= form.number_field :num_sets %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
